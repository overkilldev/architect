import React from "react";

import ContentAutocomplete from "./ContentAutocomplete";
import { render } from "setupTests";

describe("ContentAutocomplete", () => {
  it("renders with default props", () => {
    render(<ContentAutocomplete />);
  });
});
